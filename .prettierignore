# Generated by pnpm and rewritten on every install; formatting it only creates
# diff noise that the next `pnpm install` undoes.
pnpm-lock.yaml

# The local emulator's bucket. Its own `.gitignore` keeps it out of git, but
# Prettier reads only the root ignore files — so an uploaded H5P library.json,
# which is stored as compact JSON, would fail `pnpm lint` for whoever ran the
# emulators last.
docker/storage/data/
