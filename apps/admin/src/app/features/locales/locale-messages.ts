/**
 * Shared by the list and the form so both screens explain the restriction the
 * API enforces in the same words, and neither offers a control whose only
 * possible outcome is a 409.
 */
export const DEFAULT_LOCALE_TOOLTIP = 'The default locale cannot be disabled or deleted.';
