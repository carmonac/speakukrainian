{
  "name": "@speakukrainian/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "seed:admin": "node --env-file-if-exists=../.env seed-admin.ts",
    "h5p:fetch": "node fetch-h5p-core.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.7.0",
    "@speakukrainian/shared": "workspace:*",
    "firebase-admin": "^13.6.0",
    "yauzl-promise": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "~6.0.0",
    "vitest": "^3.2.4"
  }
}
